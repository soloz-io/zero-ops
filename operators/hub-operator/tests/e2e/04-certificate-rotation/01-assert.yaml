---
# AC 19.3: Assert DB_ROOT_CERT updated in infisical-secrets
# KUTTL will poll until this matches or timeout
apiVersion: v1
kind: Secret
metadata:
  name: infisical-secrets
  namespace: platform-data
  annotations:
    # Verify the secret was updated (resourceVersion will have changed)
    ops.nutgraf.in/rotation-test: "true"

---
# Rotated infisical DB credentials fixture
# Committed to Git — ArgoCD syncs this to trigger password rotation
# The operator watches secrets with label ops.nutgraf.in/db-credentials=true
apiVersion: v1
kind: Secret
metadata:
  name: infisical-db-credentials
  namespace: platform-data
  labels:
    ops.nutgraf.in/db-credentials: "true"
type: Opaque
stringData:
  username: infisical
  password: rotated-test-password-32chars-ok

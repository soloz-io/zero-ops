apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: meters.billing.nutgraf.in
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
spec:
  group: billing.nutgraf.in
  names:
    kind: Meter
    listKind: MeterList
    plural: meters
    shortNames:
    - meter
    singular: meter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tenantId
      name: Tenant
      type: string
    - jsonPath: .spec.slug
      name: Slug
      type: string
    - jsonPath: .spec.aggregation
      name: Aggregation
      type: string
    - jsonPath: .status.conditions[?(@.type=="Synced")].status
      name: Synced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Meter is the Schema for the meters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MeterSpec defines the desired state of Meter
            properties:
              aggregation:
                description: Aggregation method (COUNT, SUM, MAX, MIN, AVG)
                enum:
                - COUNT
                - SUM
                - MAX
                - MIN
                - AVG
                type: string
              description:
                description: Description of the meter
                type: string
              eventType:
                description: EventType is the OTLP event type to meter
                type: string
              groupBy:
                additionalProperties:
                  type: string
                description: GroupBy defines aggregation dimensions
                type: object
              slug:
                description: Slug is the unique identifier for the meter
                maxLength: 64
                pattern: ^[a-z0-9_]+$
                type: string
              tenantId:
                description: TenantID is the OpenMeter namespace (tenant identifier)
                pattern: ^[a-f0-9]{8}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{4}-[a-f0-9]{12}$
                type: string
              valueProperty:
                description: ValueProperty is the JSON path to the value field
                type: string
            required:
            - aggregation
            - eventType
            - slug
            - tenantId
            type: object
          status:
            description: MeterStatus defines the observed state of Meter
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Meter's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is the last time the meter was synced to
                  OpenMeter
                format: date-time
                type: string
              openMeterId:
                description: OpenMeterID is the ID assigned by OpenMeter
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

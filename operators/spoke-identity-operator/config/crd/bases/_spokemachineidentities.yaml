---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: spokemachineidentities.identity.zeroops.io
spec:
  group: identity.zeroops.io
  names:
    kind: SpokeMachineIdentity
    listKind: SpokeMachineIdentityList
    plural: spokemachineidentities
    shortNames:
    - smi
    - smids
    singular: spokemachineidentity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.spokeRef.name
      name: Spoke
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SpokeMachineIdentity manages a Machine Identity in Infisical for a Spoke cluster.
          It is the declarative representation of a Spoke's infrastructure identity.
          The Spoke Identity Operator reconciles this CR against the Infisical API.

          NOTE: This is the initial implementation of the Platform Identity Domain.
          It may evolve to support full identity lifecycle (rotation, revocation, attestation, audit)
          as defined in a future ADR.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              infisical:
                description: Infisical configures the Infisical provider
                properties:
                  authMethod:
                    default: universal-auth
                    type: string
                  clientSecretTTL:
                    default: 90d
                    type: string
                  organizationID:
                    type: string
                  projectID:
                    type: string
                type: object
              revocationPolicy:
                description: RevocationPolicy defines credential revocation settings
                properties:
                  gracePeriod:
                    default: 72h
                    type: string
                  revokeOnDelete:
                    default: true
                    type: boolean
                required:
                - revokeOnDelete
                type: object
              rotationPolicy:
                description: RotationPolicy defines credential rotation settings
                properties:
                  enabled:
                    default: true
                    type: boolean
                  interval:
                    default: 60d
                    type: string
                  overlapPeriod:
                    default: 24h
                    type: string
                required:
                - enabled
                type: object
              secretName:
                description: |-
                  SecretName overrides the default CRS wrapper Secret name.
                  Default: "<spokeRef.name>-machine-identity"
                type: string
              spokeRef:
                description: SpokeRef identifies the SpokePool this identity belongs
                  to
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
            required:
            - infisical
            - spokeRef
            type: object
          status:
            properties:
              clientID:
                description: ClientId is the Infisical Universal Auth client ID
                type: string
              clientSecretIDs:
                description: ClientSecretIds tracks active client secrets for lifecycle
                  management
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest state observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identityID:
                description: IdentityId is the Infisical Machine Identity ID
                type: string
              lastRotated:
                description: LastRotated is when the most recent rotation occurred
                format: date-time
                type: string
              nextRotation:
                description: NextRotation is when the next rotation is due
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

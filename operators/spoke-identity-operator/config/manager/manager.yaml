apiVersion: apps/v1
kind: Deployment
metadata:
  name: spoke-identity-operator
  namespace: platform-ops
  labels:
    control-plane: spoke-identity-operator
spec:
  selector:
    matchLabels:
      control-plane: spoke-identity-operator
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: spoke-identity-operator
    spec:
      serviceAccountName: spoke-identity-operator
      containers:
      - name: manager
        image: ghcr.io/soloz-io/spoke-identity-operator:latest
        env:
        - name: INFISICAL_CLIENT_ID
          valueFrom:
            secretKeyRef:
              name: spoke-identity-operator-infisical
              key: clientId
        - name: INFISICAL_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              name: spoke-identity-operator-infisical
              key: clientSecret
        - name: INFISICAL_ORGANIZATION_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['infisical.nutgraf.in/organizationId']
        - name: INFISICAL_PROJECT_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.annotations['infisical.nutgraf.in/projectId']
        args:
        - --infisical-url=http://infisical-standalone-infisical.platform-security.svc:8080
        - --infisical-org-id=$(INFISICAL_ORGANIZATION_ID)
        - --infisical-project-id=$(INFISICAL_PROJECT_ID)
        securityContext:
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10

apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  labels:
    app.kubernetes.io/name: cert-operator
    app.kubernetes.io/managed-by: kustomize
  name: cert-operator-leader-election-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: cert-operator-leader-election-role
subjects:
- kind: ServiceAccount
  name: cert-operator
  namespace: system

name: Publish zero-ops-auth

on:
  push:
    branches: [main]
    paths:
      - "packages/auth/**"
  pull_request:
    branches: [main]
    paths:
      - "packages/auth/**"

jobs:
  validate-and-build:
    name: Validate & Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"
      - uses: pnpm/action-setup@v4
        with:
          version: 11.1.3
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm --filter zero-ops-auth typecheck
      - name: Test
        run: pnpm --filter zero-ops-auth test
      - name: Build
        run: pnpm --filter zero-ops-auth build

  publish:
    name: Publish to npm
    needs: validate-and-build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22.13.0"
          registry-url: "https://registry.npmjs.org"
      - uses: pnpm/action-setup@v4
        with:
          version: 11.1.3
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build
        run: pnpm --filter zero-ops-auth build
      - name: Check if version changed
        id: version-check
        working-directory: packages/auth
        shell: bash
        run: |
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          LATEST_VERSION=$(npm view "zero-ops-auth" version 2>/dev/null || echo "0.0.0")
          echo "Current version: $CURRENT_VERSION"
          echo "Latest published: $LATEST_VERSION"
          if [ "$CURRENT_VERSION" != "$LATEST_VERSION" ]; then
            echo "should_publish=true" >> $GITHUB_OUTPUT
            echo "New version detected, will publish"
          else
            echo "should_publish=false" >> $GITHUB_OUTPUT
            echo "Version already published, skipping"
          fi
      - name: Publish to npm
        if: steps.version-check.outputs.should_publish == 'true'
        run: pnpm --filter zero-ops-auth publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

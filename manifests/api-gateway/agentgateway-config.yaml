apiVersion: v1
kind: ConfigMap
metadata:
  name: agentgateway-config
  namespace: api-gateway
data:
  config.yaml: |
    binds:
    - port: 3000
      listeners:
      - routes:
        - matches:
          - path:
              exact: /health
          policies:
            directResponse:
              status: 200
              body: "OK"
        - backends:
          - mcp:
              targets:
              - name: demo-echo
                mcp:
                  host: http://demo-echo.api-gateway.svc.cluster.local:8080/mcp
          matches:
          - path:
              pathPrefix: /mcp
          - path:
              exact: /.well-known/oauth-protected-resource
          - path:
              exact: /.well-known/oauth-authorization-server
          policies:
            cors:
              allowHeaders:
              - mcp-protocol-version
              - content-type
              - authorization
              allowOrigins:
              - '*'
              exposeHeaders:
              - Mcp-Session-Id
            mcpAuthentication:
              mode: strict
              issuer: https://auth.zero-ops.io
              audiences:
              - https://api.zero-ops.io
              jwks:
                url: http://hydra-public.ory-system.svc.cluster.local:4444/.well-known/jwks.json
              resourceMetadata:
                resource: https://api.zero-ops.io
                scopesSupported:
                - tenant:read
                - tenant:write
                - cluster:read
                - cluster:write
                - offline_access
                - openid
                bearerMethodsSupported:
                - header

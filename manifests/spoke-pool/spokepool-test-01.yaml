---
# Test SpokePool XR for Phase 1 validation
# Apply via GitOps: commit to main branch and let ArgoCD sync
apiVersion: zero-ops.io/v1alpha1
kind: SpokePool
metadata:
  name: spokepool-test-01
  namespace: default
  labels:
    environment: test
    phase: phase-1-validation
spec:
  region: fsn1
  nodePool:
    count: 3
    instanceType: cx31
  maxTenantCapacity: 100

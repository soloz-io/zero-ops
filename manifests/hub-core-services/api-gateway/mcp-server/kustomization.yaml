apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml
- ghcr-pull-secret-es.yaml

# Pinned by digest, not by tag — see the mcp-server workflow, which rewrites this
# with `kustomize edit set image` after each build.
images:
- digest: sha256:990bfec09d9e6e89ac6e375f8137d014ce870ad19cec61b40bbfb464838928e0
  name: ghcr.io/soloz-io/zero-ops/mcp-server

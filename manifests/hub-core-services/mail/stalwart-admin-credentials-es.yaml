apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: stalwart-admin-credentials
  namespace: platform-mail
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical-backend
    kind: ClusterSecretStore
  target:
    name: stalwart-admin-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      engineVersion: v2
      mergePolicy: Replace
      data:
        password: "{{ .password }}"
  data:
    - secretKey: password
      remoteRef:
        key: hub-stalwart-admin-password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None

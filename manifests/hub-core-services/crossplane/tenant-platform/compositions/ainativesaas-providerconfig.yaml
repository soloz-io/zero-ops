# Deleted: This ProviderConfig is natively managed by the Spoke catalog
# via spoke-catalog/infra/crossplane-providerconfig-sql.yaml.
# The Hub-level Object resource had an empty providerConfigRef.name,
# causing continuous reconciliation failures (6,484+ retries).
# The Spoke cluster already deploys a fully functional default ProviderConfig locally.

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hub-db-credentials
  namespace: platform-data
  annotations:
spec:
  refreshInterval: 1h

  secretStoreRef:
    name: infisical-backend
    kind: ClusterSecretStore

  target:
    name: hub-db-credentials
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
        url: "postgresql://{{ .username }}:{{ .password }}@platform-db-rw.platform-data.svc.cluster.local:5432/hub?sslmode=require"

  data:
    - secretKey: username
      remoteRef:
        key: hub-centralized-db-username
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
    - secretKey: password
      remoteRef:
        key: hub-centralized-db-password
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None

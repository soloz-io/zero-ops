apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: windows-cluster-engine
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: zero-ops-system
      name: windows-engine-credentials
      key: kubeconfig

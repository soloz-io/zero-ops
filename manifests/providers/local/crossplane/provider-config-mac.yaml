apiVersion: kubernetes.crossplane.io/v1alpha1
kind: ProviderConfig
metadata:
  name: mac-cluster-engine
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: zero-ops-system
      name: mac-engine-credentials
      key: kubeconfig

apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: platform-db
  namespace: zero-ops-system
spec:
  instances: 3
  storage:
    size: 20Gi
    storageClass: hcloud-volumes

  enableSuperuserAccess: true

  certificates:
    serverAltDNSNames:
      - platform-db-pooler
      - platform-db-pooler.zero-ops-system
      - platform-db-pooler.zero-ops-system.svc
      - platform-db-pooler.zero-ops-system.svc.cluster.local

  bootstrap:
    initdb:
      database: postgres
      owner: postgres
      postInitSQL:
        # Databases only - users created via setup-platform-roles-job
        - CREATE DATABASE control_plane;
        - CREATE DATABASE hub;
        - CREATE DATABASE infisical;

apiVersion: batch/v1
kind: Job
metadata:
  name: infisical-migrations-v1
  namespace: zero-ops-system
  annotations:
    # ArgoCD Sync Hook: Runs on every sync operation
    # BeforeHookCreation: Deletes old job before creating new one (handles immutable Job specs)
    # Runs after setup-platform-roles (sync-wave 3) creates the infisical role
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    argocd.argoproj.io/sync-wave: "4"
spec:
  ttlSecondsAfterFinished: 60
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      initContainers:
      - name: wait-for-db
        image: postgres:16
        command:
          - sh
          - -c
          - |
            until pg_isready -h platform-db-rw -p 5432; do
              echo "Waiting for database..."
              sleep 2
            done
      containers:
      - name: migrate
        image: migrate/migrate:latest
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: infisical-db-credentials
              key: url
        command:
          - sh
          - -c
          - migrate -path=/migrations -database="${DATABASE_URL}" up
        volumeMounts:
        - name: migrations
          mountPath: /migrations
      volumes:
      - name: migrations
        configMap:
          name: infisical-migrations

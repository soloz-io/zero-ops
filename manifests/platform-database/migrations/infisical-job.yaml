apiVersion: batch/v1
kind: Job
metadata:
  name: infisical-migrations-v1
  namespace: zero-ops-system
  annotations:
    # CRITICAL: Runs Infisical database schema migrations after roles are created
    # Pattern: Uses sync-wave with Replace=true instead of Sync hooks to avoid ArgoCD deadlocks
    # Replace=true: Deletes and recreates the job when spec changes (handles immutable pod templates)
    # Depends on: setup-platform-roles (sync-wave 3) creating the infisical role
    argocd.argoproj.io/sync-wave: "4"
    argocd.argoproj.io/sync-options: Replace=true
spec:
  ttlSecondsAfterFinished: 60
  backoffLimit: 3
  template:
    spec:
      restartPolicy: OnFailure
      initContainers:
      - name: wait-for-db
        image: postgres:16
        command:
          - sh
          - -c
          - |
            until pg_isready -h platform-db-rw -p 5432; do
              echo "Waiting for database..."
              sleep 2
            done
      containers:
      - name: migrate
        image: migrate/migrate:latest
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: infisical-db-credentials
              key: url
        command:
          - sh
          - -c
          - migrate -path=/migrations -database="${DATABASE_URL}" up
        volumeMounts:
        - name: migrations
          mountPath: /migrations
      volumes:
      - name: migrations
        configMap:
          name: infisical-migrations

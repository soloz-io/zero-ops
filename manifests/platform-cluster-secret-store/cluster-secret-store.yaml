apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: infisical-backend
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  provider:
    infisical:
      hostAPI: "http://platform-infisical-infisical-standalone-infisical.zero-ops-system.svc:8080/api"
      secretsScope:
        projectSlug: "platform"
        environmentSlug: "prod"
      auth:
        universalAuthCredentials:
          clientId:
            name: infisical-auth
            key: client-id
            namespace: external-secrets-system
          clientSecret:
            name: infisical-auth
            key: client-secret
            namespace: external-secrets-system

# ADR-021 Blocker 6 / ADR-022: Dedicated migration ServiceAccount.
# Scoped to migration workloads only — no access to runtime secrets.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: migration-sa
  annotations:
    argocd.argoproj.io/hook: PreSync
  labels:
    app.kubernetes.io/component: migration

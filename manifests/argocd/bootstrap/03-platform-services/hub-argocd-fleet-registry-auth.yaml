apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-argocd-fleet-registry-auth
  namespace: platform-ops
  annotations:
spec:
  project: default
  source:
    repoURL: https://github.com/soloz-io/zero-ops
    targetRevision: HEAD
    path: manifests/hub-core-services/argocd-fleet-registry-auth
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: platform-ops
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 15
      backoff:
        duration: 30s
        maxDuration: 10m

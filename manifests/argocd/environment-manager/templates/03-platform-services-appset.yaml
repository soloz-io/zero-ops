{{ if .Values.deploy.boundary03 }}
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: 03-platform-services
  namespace: platform-ops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  syncPolicy:
    # ADR-046 incident (2026-08-18): preserveResourcesOnDeletion: false allowed
    # ArgoCD pruning to cascade-delete platform Namespaces (tracked in the
    # catalog) on app removal, which in turn cascade-deleted untracked
    # CRS-delivered Secrets (infisical-auth). Deletion must never be the
    # mechanism that removes platform state — rotate spokes instead.
    preserveResourcesOnDeletion: true
  generators:
    - list:
        elements:
          - appName: 'api-gateway'
            destinationNamespace: 'platform-controlplane'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/api-gateway'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          # - appName: 'clickhouse-operator'
          #   destinationNamespace: 'platform-ops'
          #   isPlatformOwned: 'false'
          #   repoURL: 'https://helm.altinity.com'
          #   targetRevision: '0.26.3'
          #   path: ''
          #   chart: 'altinity-clickhouse-operator'
          #   directoryRecurse: ''
          #   directoryInclude: ''
          #   helmValues: |
          #     operator:
          #       env:
          #         - name: WATCH_NAMESPACES
          #           value: ".*"
          #   helmReleaseName: 'clickhouse-operator'
          #   ignoreDifferencesNamespace: ''
          - appName: 'platform-argocd-fleet-registry-auth'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/argocd-fleet-registry-auth'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-argocd-github-auth'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/argocd-github-auth'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-capi2argo'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/capi2argo'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'hub-environment'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/environments/{{ .Values.environmentSlug }}'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'infrastructure-provider'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/providers/{{ .Values.provider }}'
            chart: ''
            directoryRecurse: 'false'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'grafana-alloy'
            destinationNamespace: 'platform-observability'
            isPlatformOwned: 'false'
            repoURL: 'https://github.com/soloz-io/zero-ops.git'
            targetRevision: 'main'
            path: 'manifests/hub-core-services/grafana-alloy'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-infisical'
            destinationNamespace: 'platform-security'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/infisical'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''

          - appName: 'hub-operator'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'operators/hub-operator/config/default'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'prometheus-operator'
            destinationNamespace: 'platform-observability'
            isPlatformOwned: 'false'
            repoURL: 'https://prometheus-community.github.io/helm-charts'
            targetRevision: '82.15.0'
            path: ''
            chart: 'kube-prometheus-stack'
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: |
              prometheus:
                enabled: false
              alertmanager:
                enabled: false
              grafana:
                enabled: false
              prometheusOperator:
                enabled: true
              kubeStateMetrics:
                enabled: false
              nodeExporter:
                enabled: false
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          # - appName: 'victoria-metrics-alerts'
          #   destinationNamespace: 'platform-observability'
          #   isPlatformOwned: 'true'
          #   repoURL: 'https://github.com/soloz-io/zero-ops'
          #   targetRevision: ''
          #   path: 'manifests/hub-core-services/victoriametrics-alerts'
          #   chart: ''
          #   directoryRecurse: ''
          #   directoryInclude: ''
          #   helmValues: ''
          #   helmReleaseName: ''
          #   ignoreDifferencesNamespace: ''
          # - appName: 'platform-victoriametrics-infisical'
          #   destinationNamespace: 'platform-observability'
          #   isPlatformOwned: 'true'
          #   repoURL: 'https://github.com/soloz-io/zero-ops'
          #   targetRevision: ''
          #   path: 'manifests/hub-core-services/victoriametrics'
          #   chart: ''
          #   directoryRecurse: 'true'
          #   directoryInclude: 'infisical-integration.yaml'
          #   helmValues: ''
          #   helmReleaseName: ''
          #   ignoreDifferencesNamespace: ''
          # - appName: 'victoriametrics-ingress'
          #   destinationNamespace: 'platform-observability'
          #   isPlatformOwned: 'false'
          #   repoURL: 'https://github.com/soloz-io/zero-ops.git'
          #   targetRevision: 'main'
          #   path: 'manifests/hub-core-services/victoriametrics'
          #   chart: ''
          #   directoryRecurse: 'true'
          #   directoryInclude: 'ingress.yaml'
          #   helmValues: ''
          #   helmReleaseName: ''
          #   ignoreDifferencesNamespace: ''
          # - appName: 'victoria-metrics-operator'
          #   destinationNamespace: 'platform-observability'
          #   isPlatformOwned: 'false'
          #   repoURL: 'https://victoriametrics.github.io/helm-charts'
          #   targetRevision: '0.59.3'
          #   path: ''
          #   chart: 'victoria-metrics-operator'
          #   directoryRecurse: ''
          #   directoryInclude: ''
          #   helmValues: ''
          #   helmReleaseName: ''
          #   ignoreDifferencesNamespace: ''
          # - appName: 'victoria-metrics-cluster'
          #   destinationNamespace: 'platform-observability'
          #   isPlatformOwned: 'false'
          #   repoURL: 'https://victoriametrics.github.io/helm-charts'
          #   targetRevision: '0.37.0'
          #   path: ''
          #   chart: 'victoria-metrics-cluster'
          #   directoryRecurse: ''
          #   directoryInclude: ''
          #   helmValues: |
          #     vmselect:
          #       replicaCount: 1
          #       resources:
          #         requests:
          #           cpu: 250m
          #           memory: 256Mi
          #         limits:
          #           cpu: 1000m
          #           memory: 2Gi
          #     vminsert:
          #       replicaCount: 1
          #       resources:
          #         requests:
          #           cpu: 250m
          #           memory: 256Mi
          #         limits:
          #           cpu: 1000m
          #           memory: 2Gi
          #     vmstorage:
          #       replicaCount: 1
          #       persistentVolume:
          #         enabled: true
          #         size: 50Gi
          #       resources:
          #         requests:
          #           cpu: 500m
          #           memory: 512Mi
          #         limits:
          #           cpu: 2000m
          #           memory: 4Gi
          #     retentionPeriod: 30d
          #   helmReleaseName: ''
          #   ignoreDifferencesNamespace: ''
          - appName: 'kube-sbt-api'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops.git'
            targetRevision: ''
            path: 'manifests/hub-core-services/kube-sbt-api'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'network-policies'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/identity'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: 'network-policies.yaml'
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'ingress-nginx-controller'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'false'
            repoURL: 'https://kubernetes.github.io/ingress-nginx'
            targetRevision: '4.12.1'
            path: ''
            chart: 'ingress-nginx'
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: |
              controller:
                ingressClassResource:
                  enabled: true
                  default: false
                # admission webhook disabled: (1) the chart's cert-gen/patch
                # Helm hook jobs don't run under ArgoCD (empty caBundle broke
                # every Ingress create with a TLS trust error), and (2) the
                # webhook rejects cert-manager's HTTP-01 solver Ingress
                # ('path cannot be used with pathType Exact'), blocking ACME
                # issuance. cert-manager relies on this for Let's Encrypt.
                admissionWebhooks:
                  enabled: false
                service:
                  enabled: true
                  type: LoadBalancer
                  annotations:
                    load-balancer.hetzner.cloud/network-zone: eu-central
                    load-balancer.hetzner.cloud/type: lb11
              defaultBackend:
                enabled: false
            helmReleaseName: 'ingress-nginx'
            ignoreDifferencesNamespace: ''
          - appName: 'ingress-config'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/ingress'
            chart: ''
            # No directoryRecurse: with recurse:true ArgoCD lists every file
            # including kustomization.yaml and tries to apply it as a
            # Kustomization CR (not installed) → SyncFailed. Without it,
            # ArgoCD auto-detects kustomize from kustomization.yaml which
            # already lists exactly {cluster-issuer.yaml,ingress.yaml}.
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-spoke-bootstrap-templates'
            destinationNamespace: 'platform-capi'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops.git'
            targetRevision: ''
            path: 'manifests/spoke/spoke-bootstrap'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-spoke-pool-xrds'
            destinationNamespace: 'default'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/spoke/xrds'
            chart: ''
            directoryRecurse: ''
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-spoke-pools'
            destinationNamespace: 'platform-ops'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/spoke/spoke-pools/{{ .Values.environmentSlug }}/{{ .Values.provider }}{{ if .Values.topology }}/{{ .Values.topology }}{{ end }}'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
          - appName: 'platform-tenant-platform'
            destinationNamespace: 'default'
            isPlatformOwned: 'true'
            repoURL: 'https://github.com/soloz-io/zero-ops'
            targetRevision: ''
            path: 'manifests/hub-core-services/crossplane/tenant-platform'
            chart: ''
            directoryRecurse: 'true'
            directoryInclude: ''
            helmValues: ''
            helmReleaseName: ''
            ignoreDifferencesNamespace: ''
  template:
    metadata:
      name: '{{"{{"}} .appName {{"}}"}}'
      namespace: platform-ops
    spec:
      project: default
      source:
        repoURL: '{{"{{"}} .repoURL {{"}}"}}'
        targetRevision: '{{"{{"}} if eq .isPlatformOwned "true" {{"}}"}}{{ .Values.environmentRevision }}{{"{{"}} else {{"}}"}}{{"{{"}} .targetRevision {{"}}"}}{{"{{"}} end {{"}}"}}'
        path: '{{"{{"}} .path {{"}}"}}'
        chart: '{{"{{"}} .chart {{"}}"}}'
        helm:
          releaseName: '{{"{{"}} .helmReleaseName {{"}}"}}'
          values: '{{"{{"}} .helmValues {{"}}"}}'

      destination:
        server: https://kubernetes.default.svc
        namespace: '{{"{{"}} .destinationNamespace {{"}}"}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
  templatePatch: |
    {{"{{-"}} if eq .directoryRecurse "true" {{"}}"}}
    spec:
      source:
        directory:
          recurse: true
    {{"{{-"}} end {{"}}"}}
    {{"{{-"}} if eq .ignoreDifferencesNamespace "true" {{"}}"}}
    spec:
      ignoreDifferences:
        - kind: Namespace
          name: '{{"{{"}} .destinationNamespace {{"}}"}}'
          jsonPointers:
            - /metadata/annotations
            - /metadata/labels
    {{"{{-"}} end {{"}}"}}
    {{"{{-"}} if eq .appName "platform-infisical" {{"}}"}}
    spec:
      ignoreDifferences:
        # ROOT CAUSE of recurring Infisical migration-lock failures.
        #
        # The chart stamps a wall-clock timestamp into the Deployment AND its pod
        # template (templates/infisical.yaml, both 1.7.5 and 1.10.0):
        #
        #     updatedAt: {{"{{"}} now | date "2006-01-01 MST 15:04:05" {{"}}"}}
        #
        # Every render therefore differs from the live object, so ArgoCD sees the
        # Deployment as permanently OutOfSync and self-heal rewrites it. Each rewrite
        # changes the pod template, producing a new ReplicaSet and killing the
        # running pod — observed as generation=4 within 28 minutes and a stream of
        # short-lived pods.
        #
        # That is fatal here because Infisical runs its schema migrations INSIDE the
        # app pod at boot. A pod killed mid-migration leaves
        # infisical_migrations_lock.is_locked = 1, and every later pod then refuses
        # to start with "Migration table is already locked". Nothing self-heals.
        #
        # On a hybrid hub the window is wide: the image lands on a home-lab node over
        # a domestic link and the pull alone ran past 4m26s, so a sync almost always
        # lands mid-pull or mid-migration.
        #
        # Ignoring the annotation makes the Deployment converge, which stops the
        # rollout loop at its source. The startupProbe patch in the infisical
        # kustomization handles the other half (readiness killing a slow migration);
        # the migration-unlock init container is the last-resort cleanup.
        - group: apps
          kind: Deployment
          name: infisical-standalone-infisical
          jsonPointers:
            - /metadata/annotations/updatedAt
            - /spec/template/metadata/annotations/updatedAt
    {{"{{-"}} end {{"}}"}}
    {{"{{-"}} if eq .appName "ingress-nginx-controller" {{"}}"}}
    spec:
      ignoreDifferences:
        - group: admissionregistration.k8s.io
          kind: ValidatingWebhookConfiguration
          name: ingress-nginx-admission
          jsonPointers:
            - /webhooks/0/clientConfig/caBundle
    {{"{{-"}} end {{"}}"}}
    {{"{{-"}} if eq .appName "clickhouse-operator" {{"}}"}}
    spec:
      ignoreDifferences:
        - group: apps
          kind: Deployment
          jsonPointers:
            - /spec/template/spec/containers/0/resources
            - /spec/progressDeadlineSeconds
    {{"{{-"}} end {{"}}"}}
    {{"{{-"}} if eq .appName "infrastructure-provider" {{"}}"}}
    spec:
      ignoreDifferences:
        # CAPI webhook defaulting adds namespace refs and empty metadata
        # blocks to ClusterClass refs/variables after creation.
        - group: cluster.x-k8s.io
          kind: ClusterClass
          jqPathExpressions:
            - .spec.controlPlane.ref.namespace
            - .spec.controlPlane.metadata
            - .spec.infrastructure.ref.namespace
            - .spec.variables[].metadata
            - .spec.workers.machineDeployments[].template.infrastructure.ref.namespace
            - .spec.workers.machineDeployments[].template.bootstrap.ref.namespace
            - .spec.workers.machineDeployments[].template.metadata
        # External Secrets controller adds finalizers, defaulted fields and
        # status to ExternalSecrets after creation.
        - group: external-secrets.io
          kind: ExternalSecret
          jqPathExpressions:
            - .metadata.finalizers
            - .spec.data[].remoteRef.conversionStrategy
            - .spec.data[].remoteRef.decodingStrategy
            - .spec.data[].remoteRef.metadataPolicy
            - .spec.target.creationPolicy
            - .spec.target.deletionPolicy
            - .spec.target.mergePolicy
            - .spec.target.template.engineVersion
            - .spec.binding
            - .status
    {{"{{-"}} end {{"}}"}}
---

---
# ADR-047: platform-spoke-catalog — deploys the platform's spoke capability
# catalog (CRDs, operators, cert-manager, infisical-issuer, ESO secret store,
# crossplane providers, CNPG, kyverno ABI, agent-sandbox, sandbox netpol) to
# every registered pool spoke.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: platform-spoke-catalog
  namespace: platform-ops
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  syncPolicy:
    # ADR-046 incident (2026-08-18): preserveResourcesOnDeletion: false allowed
    # ArgoCD pruning to cascade-delete platform Namespaces (tracked in the
    # catalog) on app removal, which in turn cascade-deleted untracked
    # CRS-delivered Secrets (infisical-auth). Deletion must never be the
    # mechanism that removes platform state — rotate spokes instead.
    preserveResourcesOnDeletion: true

  generators:
    - clusters:
        selector:
          matchLabels:
            spoke-type: pool

  template:
    metadata:
      name: 'platform-spoke-catalog-{{"{{"}} .name {{"}}"}}'
      namespace: platform-ops
      labels:
        app.kubernetes.io/part-of: platform
        spoke-type: pool

    spec:
      project: default

      source:
        repoURL: https://github.com/soloz-io/zero-ops
        targetRevision: '{{ .Values.environmentRevision }}'
        # ADR-046 §11: spoke-catalog is structured per environment and provider;
        # the CNPG overlay lives at manifests/spoke/spoke-catalog/environments/<env>/<provider>.
        # The cluster generator exposes labels under .metadata.labels; fail closed
        # on any provider other than hybrid/hetzner (index + fail primitive also
        # covers a spoke whose provider take-along label is missing).
        path: 'manifests/spoke/spoke-catalog/environments/{{ .Values.environmentSlug }}/{{"{{"}} if eq (index .metadata.labels "provider") "hybrid" {{"}}"}}hybrid{{"{{"}} else if eq (index .metadata.labels "provider") "hetzner" {{"}}"}}hetzner{{"{{"}} else {{"}}"}}{{"{{"}} fail "unknown provider label: spoke-catalog path" {{"}}"}}{{"{{"}} end {{"}}"}}'
        kustomize:
          patches:
            # Inject the per-spoke crossplane-admin password key (ADR-003/tenant-db
            # bootstrap). The hub-operator generates <spoke>-crossplane-admin-password
            # in the hub-secrets project; the ExternalSecret remoteRef must match
            # that exact key for the spoke CNPG/provider-sql bootstrap to resolve.
            - target:
                kind: ExternalSecret
                name: crossplane-admin-credentials
              patch: |
                - op: replace
                  path: /spec/data/0/remoteRef/key
                  value: '{{"{{"}}.name{{"}}"}}-crossplane-admin-password'
            # ADR-014 backup contract: per-spoke barman prefix. destinationPath is
            # a Cluster property — ScheduledBackup has no destination fields and
            # inherits this configuration.
            - target:
                kind: Cluster
                name: shared-cnpg
              patch: |
                - op: replace
                  path: /spec/backup/barmanObjectStore/destinationPath
                  value: 's3://spoke-pool-backups/{{"{{"}}.name{{"}}"}}/'
            # ADR-014 backup contract: per-spoke S3 keys in the hub-secrets
            # project at /spoke-pool/<spoke>/shared/. The hcloud-token is an
            # HCloud API token, NOT a Hetzner Object Storage key — it fails with
            # InvalidAccessKeyId (verified live 2026-08-18).
            - target:
                kind: ExternalSecret
                name: s3-credentials
              patch: |
                - op: replace
                  path: /spec/data/0/remoteRef/key
                  value: '/spoke-pool/{{"{{"}}.name{{"}}"}}/shared/S3_ACCESS_KEY_ID'
                - op: replace
                  path: /spec/data/1/remoteRef/key
                  value: '/spoke-pool/{{"{{"}}.name{{"}}"}}/shared/S3_SECRET_ACCESS_KEY'
            # WS3: external-dns blast-radius + record ownership.
            # --domain-filter scopes which zone this spoke may write, so a dev
            # spoke can never touch prod records. --txt-owner-id keys the TXT
            # registry; two spokes sharing an owner id would fight over records.
            # Both are args[0]/args[1] by contract (see external-dns.yaml) — they
            # are pinned first precisely so appended flags cannot shift the index.
            # environmentSlug is a Helm value resolved at bootstrap; .name is the
            # ArgoCD cluster name resolved per generated Application.
            - target:
                kind: Deployment
                name: external-dns
              patch: |
                - op: replace
                  path: /spec/template/spec/containers/0/args/0
                  value: '--domain-filter={{ if eq .Values.environmentSlug "prod" }}nutgraf.in{{ else }}{{ .Values.environmentSlug }}.nutgraf.in{{ end }}'
                - op: replace
                  path: /spec/template/spec/containers/0/args/1
                  value: '--txt-owner-id={{"{{"}}.name{{"}}"}}'
            # Environment isolation (ADR-037). The spoke ClusterSecretStore ships
            # with environmentSlug hardcoded to "dev"
            # (manifests/spoke/spoke-catalog/infra/cluster-secret-store.yaml), so a
            # stg or prod spoke would read DEV secrets out of Infisical. The Hub's
            # environmentSlug is the authority and is a Helm value resolved at
            # bootstrap. spoke-catalog is KUSTOMIZE-rendered, not Helm, so
            # `{{ .Values.x }}` cannot be written into the manifest itself — the
            # substitution has to happen here.
            - target:
                kind: ClusterSecretStore
                name: infisical-backend
              patch: |
                - op: replace
                  path: /spec/provider/infisical/secretsScope/environmentSlug
                  value: '{{ .Values.environmentSlug }}'

      destination:
        name: '{{"{{"}} .name {{"}}"}}'
        namespace: platform-ops

      # ADR-046 §11 (Wave C): .spec.bootstrap is intentionally ignored — the
      # declarative steady-state manifest cannot represent the historical
      # creation/bootstrap mechanism of an already-initialized CNPG cluster.
      # CNPG evaluates bootstrap only at creation time and never re-reconciles
      # it, so the recovery-state manifest (Wave B) returning to initdb (Wave C)
      # stays permanently Synced.
      ignoreDifferences:
        - group: postgresql.cnpg.io
          kind: Cluster
          name: shared-cnpg
          jsonPointers:
            - /spec/bootstrap

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
          - RespectIgnoreDifferences=true
        retry:
          limit: 5
          backoff:
            duration: 5s
            factor: 2
            maxDuration: 3m
---
{{ end }}

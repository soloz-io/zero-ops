# ADR-047 Tier 1: Platform-owned default-deny for sandbox pods.
# Sandboxes run in tenant namespaces but are created by the agent-sandbox
# controller from SDK-supplied podTemplates — they are NOT covered by the
# tenant workload base netpol. This cluster-wide policy default-denies them:
# DNS + public-internet egress only (agent-vault proxy + harness), no
# cluster-internal access.
# Sandbox pods are selected by agents.x-k8s.io/sandbox, which the platform's
# ephemeral-job-operator sets on every sandbox pod it authors.
#
# This has now been wrong in BOTH directions, and the reason is the same each
# time: the selector must name the label whichever component authors the pod
# actually sets.
#
# It first selected agents.x-k8s.io/sandbox: "true" while the upstream
# agent-sandbox controller authored the pod. That controller owns the
# agents.x-k8s.io/ label space and replaced it with
# agents.x-k8s.io/sandbox-name-hash, so the selector matched nothing (verified
# 2026-08-31 on a live sandbox). It was changed to select the hash.
#
# ADR-052's 2026-09-02 amendment moved authorship to the ephemeral-job-operator,
# which propagates the request's own labels onto the pod. The hash is no longer
# set by anything, so selecting it matched nothing again — verified on a live
# sandbox pod carrying {agents.x-k8s.io/sandbox: "true", ...} and no hash.
#
# Both failures are silent. A CiliumClusterwideNetworkPolicy selecting zero
# endpoints is Healthy; it is simply inert, and the pods it should default-deny
# run unconstrained with nothing in error. A selector that matches nothing is
# indistinguishable from one that matches everything it should, so this file
# should not be trusted on the strength of its status.
#
# The label is now platform-set, on a pod the platform authors, which is the
# strongest arrangement available: it is present because the operator put it
# there, and a fleet cannot omit it to escape the policy.
#
# The Cilium CRDs arrive with Cilium itself, in the CAPI ClusterResourceSet
# addon (ADR-041) -- outside this Application and outside ArgoCD entirely. So
# on a new spoke this resource is validated against an API that does not yet
# serve cilium.io/v2, and ArgoCD's dry-run rejects the whole sync before
# applying anything: "one or more synchronization tasks are not valid".
#
# That is not a slow start that retries out of. Observed 2026-09-18 on
# nutgraf-01: the catalogue's five retries ran 15:29:11 -> 15:41:18 and the
# CRD was established at 15:43:24 -- two minutes after the budget expired. All
# 351 objects were then blocked by this one, and automated sync would not try
# again, because ArgoCD refuses to re-sync a revision whose last attempt failed
# (selfHeal included). A two-minute race became a permanently wedged spoke with
# an empty database, and step 10f reported it as "the spoke's shared-cnpg has no
# ready instance".
#
# Skipping the dry-run for this one resource lets the other 350 apply and this
# one settle on a later pass, which is the same reason cluster-secret-store.yaml
# and the crossplane provider configs carry it.
apiVersion: cilium.io/v2
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: agent-sandbox-default-deny
  annotations:
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  endpointSelector:
    matchExpressions:
      - key: agents.x-k8s.io/sandbox
        operator: Exists

  egress:
    # The tenant's own database, through the pooler.
    #
    # The harness is handed a DATABASE_URL by the SDK and fails startup without
    # it — "psycopg.errors.ConnectionTimeout: connection timeout expired", then
    # "Application startup failed" — so the sandbox starts, serves nothing and
    # exits, while the workflow node sits in `running` until it times out.
    #
    # This was not needed while the policy's selector matched no pods: sandboxes
    # were governed by nothing and reached everything. Correcting the selector is
    # what made this rule necessary, and its absence is the one hole that
    # correction opened.
    #
    # Scoped to the pooler, not to platform-data: the sandbox runs tenant-authored
    # agent code, so it gets the one endpoint it must reach and not the namespace
    # around it.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: platform-data
            k8s:cnpg.io/podRole: pooler
      toPorts:
        - ports:
            - port: "5432"
              protocol: TCP

    # DNS for FQDN rules. Both backends: CoreDNS directly, or the node-local
    # cache when the redirect policy is in force. Naming only kube-dns made this
    # rule stop matching the instant that redirect landed, because Cilium
    # enforces egress on the service ENDPOINT under kube-proxy-replacement --
    # see tenant-dns-egress.yaml for the incident and the measurements.
    - toEndpoints:
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: kube-dns
        - matchLabels:
            k8s:io.kubernetes.pod.namespace: kube-system
            k8s:k8s-app: node-local-dns
      toPorts:
        - ports:
            - port: "53"
              protocol: UDP
            - port: "53"
              protocol: TCP

    # Public-internet egress for the agent-vault proxy / harness tooling.
    # STOPGAP: outbound HTTPS to any external host.
    #
    # The toFQDNs rules below are the intended expression and are kept. They
    # match nothing on this platform: Cilium builds the FQDN cache by proxying
    # the workload's DNS queries, that proxy receives nothing here, and the cache
    # stays empty — so an allowlist enforces a deny-all. Verified again on
    # 2026-09-01 with `enable-bpf-tproxy`, the fourth candidate fix and the last
    # configuration lever; the cache stayed empty. See enhancements 2026-08-31
    # items 9, 10, 15 and ADR-046 addendum 30.
    #
    # Without this the agent cannot reach its model provider at all: the request
    # leaves through agent-vault's transparent proxy, the proxy cannot reach
    # upstream, and it answers 502 — surfacing as `graph_execution_failed:
    # bad gateway` and reaching the browser as "Something went wrong".
    #
    # This IS a real widening. A sandbox runs tenant-authored agent code and this
    # grants it outbound HTTPS anywhere. Two things bound how far: the FQDN rules
    # currently match nothing, so the posture it replaces is no external access
    # rather than the allowlist; and that allowlist already contains github.com,
    # which is itself an exfiltration path. Neither makes this equivalent to the
    # allowlist, and it should not be described as though it were.
    #
    # 443 only, egress only. There is no ingress section in this policy, so
    # nothing here affects what may reach a sandbox — the SDK still reaches :3000
    # for SSE because that is ingress.
    #
    # REVISIT TRIGGER: delete this rule the moment the FQDN cache populates. The
    # toFQDNs rules below resume being operative on their own; nothing else needs
    # to change. Check with `cilium-dbg fqdn cache list` on any node.
    - toEntities:
        - world
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP

    - toFQDNs:
        - matchName: "api.github.com"
        - matchName: "github.com"
        - matchName: "api.tavily.com"
        - matchName: "api.runpod.ai"
      toPorts:
        - ports:
            - port: "443"
              protocol: TCP

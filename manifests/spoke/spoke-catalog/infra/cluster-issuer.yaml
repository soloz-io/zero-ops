---
# Root ClusterIssuer backed by Infisical PKI
# All spoke certificates are issued through this issuer
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: infisical-ca
spec:
  ca:
    secretName: infisical-ca-key-pair

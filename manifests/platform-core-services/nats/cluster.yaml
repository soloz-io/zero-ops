apiVersion: v1
kind: Service
metadata:
  name: nats
  namespace: zero-ops-system
  labels:
    app: nats
spec:
  selector:
    app: nats
  ports:
  - name: client
    port: 4222
    targetPort: 4222
  - name: monitoring
    port: 8222
    targetPort: 8222
  - name: cluster
    port: 6222
    targetPort: 6222
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nats-config
  namespace: zero-ops-system
data:
  nats.conf: |
    server_name: $POD_NAME
    listen: 0.0.0.0:4222
    http: 8222
    
    cluster {
      name: zero-ops-nats
      listen: 0.0.0.0:6222
      routes: [
        "nats://nats-0.nats.zero-ops-system.svc.cluster.local:6222",
        "nats://nats-1.nats.zero-ops-system.svc.cluster.local:6222",
        "nats://nats-2.nats.zero-ops-system.svc.cluster.local:6222"
      ]
    }
    
    jetstream {
      store_dir: /data/jetstream
      max_memory_store: 1Gi
      max_file_store: 10Gi
    }
    
    authorization {
      users = [
        { user: "agent", password: "agentpass", permissions: { publish: ["hub.platform.>"], subscribe: ["hub.platform.>"] } }
        { user: "spoke", password: "spokepass", permissions: { publish: ["spoke.>"], subscribe: ["spoke.>"] } }
      ]
    }
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nats
  namespace: zero-ops-system
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  serviceName: nats
  replicas: 3
  selector:
    matchLabels:
      app: nats
  template:
    metadata:
      labels:
        app: nats
    spec:
      containers:
      - name: nats
        image: nats:2.10
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        ports:
        - containerPort: 4222
          name: client
        - containerPort: 6222
          name: cluster
        - containerPort: 8222
          name: monitoring
        volumeMounts:
        - name: config
          mountPath: /etc/nats
        - name: data
          mountPath: /data
        command: ["nats-server", "-c", "/etc/nats/nats.conf"]
        livenessProbe:
          httpGet:
            path: /
            port: 8222
          initialDelaySeconds: 10
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /
            port: 8222
          initialDelaySeconds: 5
          periodSeconds: 10
      volumes:
      - name: config
        configMap:
          name: nats-config
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
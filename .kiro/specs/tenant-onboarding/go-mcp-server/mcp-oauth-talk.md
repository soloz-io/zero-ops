https://www.youtube-transcript.io/videos?id=mYKMwZcGynw


[Music] all right hi everybody i'm Aaron Pari from Octa happy to be here i'm going to do a quick little audience poll first because I'm really curious to know more about who is here raise your hand if you are building an MCP server okay almost almost everybody's what I thought raise your hand if you're building an MCP client okay a bunch um what about an OOTH server oh that's more than I expected okay we should talk later um so I'm going to talk about about OOTH here and uh how OOTH relates to MCP um if you've ever tried to read the OOTH spec first of all I apologize they're they're not specs are not really good tutorials they're not really meant to be tutorials they are meant to be the reference for for how this stuff works and um OOTH is not one spec ooth is a whole bunch of specs it is a bit of a mess it's it's definitely a maze um most of it you probably don't care about most of it you shouldn't need to care about um and every time there's an ecosystem that goes and builds something with OOTH they do pick and choose which parts of it to to use which is which is good um OOTH 2 a little bit of of background here ooth 2 uh is a bit messy there's a lot of history there it was published uh it was the OOTH 2 core spec was published in 2012 which is a long time ago now and over the years things have changed things about the technology and the and browsers have changed and throughout the last 13 years there's been a lot of changes to OOTH to adapt to those changes in the technology that we're using so you'll find newer RFC's that add features or remove features and uh it's definitely a bit hard to get your head around like what is this all supposed to mean so but if you actually look at the current state of things it's not nearly as bad as it actually looks in this picture it's actually a much simpler picture of just we actually just have the authorization code flow with Pixie and we have the client credentials grant which is debatable whether it's actually useful um and a couple of ways to send tokens in requests and this is what OOTH 2.1 is is meant to do it's meant to collapse the last now 13 years of specs uh and and set a new baseline for um what we're doing with OOTH and that's why I was really glad to see that when the remote spec for MCP launched they referenced OOTH 2.1 as like we're going to use the best current practice of OOTH but I want to go back in time a little bit and uh talk about how we got to OOTH in the first place to kind of talk about some of the problems that it solves you might be familiar with OOTH from these kinds of signin with buttons uh that's not exactly what OOTH was created to do it is an application of OOTH um but originally the goal of OOTH was to solve a very real problem where people were putting passwords into third party applications so you would see a new application like Yelp would launch and it would say hey uh we want to see if your friends are already on Yelp where your friends well this was before social networking was big because this was when social networking was becoming big uh your friends are in your email so Yelp is going to ask you for your email address and the log into your email go log into your account download the data that it needs and everything is happy right i mean if you see a screen like this today hopefully you have a sort of visceral reaction of like I'm not going to give my email password to random applications on the internet right uh but it was just how things were done before there was OOTH because there wasn't a better solution but I want to talk specifically about why this is bad one if you give it this password how do you take that access back you've you've given it your password which is the access to your account do you trust that application to do what it says and not store your password and use it later do you trust that app to access only the things that it said it was going to access do you trust that application to not do things like change your password um so basically what it boils down to is is trust like do you trust the application and that's kind of the situation that we're in with the the the MCP right now of all these local servers you're kind of just hoping that it does the right thing and you're giving it API keys and passwords and whatever and and crossing your fingers so ideally there's a better solution to this which is how do we let applications get access to the data they want that the user also wants to share with that application while preventing access to things it doesn't need and that's really what Oat set out to solve specifically this problem of how do we let apps access data without sharing passwords there's an analogy to this which is um when you go check into a hotel you go to the front desk and you show your ID and give them your credit card and they give you back this hotel key now that hotel key does not represent your identity it represents your access to things in the hotel that hotel key is very much like an OOTH access token an access token doesn't necessarily have to represent your identity what it actually represents is your access to resources in the hotel example the resources are the doors in the hotel your room maybe the the gym the pool the front door after 10 p.m things like that so if we look at the OOTH terms for these we have the OOTH authorization server is the one handing out key cards issuing key cards it knows how to write the right information into the card that key card is like an OOTH access token and the resources in the hotel the APIs or the MCP servers need to know how to validate the key cards and the really important thing here is that if you are using a key card at a hotel you don't actually care what's in it like you don't need to know so if you're the one that like you know got got this card from the hotel you're going around swiping it on the doors do you care if there's like what kind of data is encoded in the magstripe or whether it's NFC or whatever kind of mechanical key no you just care that when you use it at the door the door knows how to read that card and this is a lot better than for example going to a hotel and checking in and they give you a skeleton key that opens every door and they just kind of hope you do the right thing so again this was created because of this kind of trust issue which is especially relevant in the third party app scenario where one company's app is accessing a different company's API so what it ends up looking like is if you're on an application and you click login or connect to your account or whatever the button is it will send you away from the application over to the OOTH server where you log in and that's where you enter your email address or username you enter your password you do your second factor off of whatever kind and eventually uh you might see a permission prompt and eventually you get redirected back to the application this is also a really important part of of the process of asking the user for permission to share the data so again like in the third party scenario we want to make really really sure that the user knows what that application is going to be able to access and that this is then enforced by the OOTH server in that token so if you are if you're if you're if we're talking about OOTH what we're talking about is a way to make API requests if you need to know about the user who is logging in you need to do something in addition to OOTH because OOTH isn't quite enough it doesn't define all the right pieces so that's where open ID connect comes in which is an extension to OOTH that defines a way to actually identify the user and talk about about the user to the application that they're logging into so we've got an access token for API requests an ID token for uh for talking about who the user is think of the access token like a key it lets you actually go and use it to do something whereas the ID token is more like just a a statement about what happened so okay what does this have to do with with MCP well in the previous version of the or the I guess current dated version of the MCP remote spec from March 26th it defined how an MCP server behaves and how a user needs to uh interact with it to log in the um essentially it would say host some information about the about the OOTH server have a path that the user can land on when they click connect and um follow all the O2.1 best practices which is almost almost correct the problem was that if you if you do it this way you are very limited in that all your MCP server developers have to also be OOTH server developers which is not a good thing so instead what we can do is if we apply the OOTH building blocks in the way they were intended to and we split up the OOTH part from the MCP server part we end up in a much better situation so in in this way the MCP client that's trying to get the user to log in deals with first deals with the OOTH server gets the user to log in does a back and forth exchange gets a token and then takes that token to the the MCP server the resource server to go and start the actual data flow so let me walk through a quick version of this flow uh to show you kind of how it works very high level i have a lot more information online if you're curious about the details of this so it starts with a user clicking login or connect your account or whatever that client is going to then initiate well there's a bunch of discovery stuff that happens that I'm going to get to in a second but once it's ready to start the flow it will begin an OOTH request by redirecting the user's browser to the OOTH server now notice that if you're building the MCP server you haven't seen the user yet this is where they log in this is where they do regular password login two-factor off it could be redirecting out to an enterprise IDP could be redirecting to another consumer IDP whatever it is doesn't really matter to the client at this point because they're the user's browser is at the OOTH server and eventually the oath server will redirect back to the client with an authorization code which the client can then exchange with a post request for an access token now again notice that all of this stuff happened which is somewhat complicated and this is the details of the OOTH flow your MCP server hasn't seen any of it yet finally at the very end that client has the token and it can go make a request to the MCP server this whole uh redirect back and forth is in the front channel this is how we get the user in their browser and can do the things like the um the login and and redirect to um other IDPs and things like that the back channel is where the actual token exchange happens which is uh which back channel being server to server calls um or actually direct calls from like a desktop client to a server but the important thing here is that most of this flow most of this was actually the business of the MCP client so that is the part that needs to know about how to send the user out and back what the MCP server sees is really just the very very end of the flow and again I want to just reiterate like if you're building the MCP server you really shouldn't need to know much except until the very end like you can pretty much ignore the rest of this so what you're going to see is a request that comes in with an access token that's you don't really care how the access token was obtained it's just this will be the first time you see this this user you do need to validate the access token and you have a couple different ways but you're basically going to go and read the docs for whatever OOTH server you you are using there's uh again there are some standard ways to do this there are some non-standard ways to do this but at the end of the day it comes down to what does your OOTH server say for how you should be validating tokens and that's how you're going to do it couple of things um that I want to point out that are unique to somewhat unique to MCP's use of OOTH um starting with the the goals for why these were here one of the goals was to be able to configure an MCP client by giving it only the URL of the server normally when you configure OOTH clients if you're talking about setting up like a you know a sign in with Google button or whatever there's a bunch of URLs and things you have to find like the different OOTH endpoints you have to get a client ID client secret and maybe other things as well so the idea is can we make it so that you can configure a C the client by dropping in a single URL and up until actually very recently there was uh no available standards for that but thankfully there is now protected resource metadata published just a few weeks ago in the OOTH working group um defines a way for an an API server resource server or MCP server to point to a file that describes the server so uh there's a bunch of properties that you can put in in the metadata file but the important one for the purposes of this discussion is the location of the OOS server that that resource server will use so then the client can uh can you know start the MCB connection get this error find the URL go fetch that metadata find the OOTH server um and go fetch the OOS server metadata to find out more information about the OOTH server and then it has what it needs to be able to actually start the OOTH flow the other goal was to enable people to build MCP servers that can be used by clients that have never before or clients that have never before seen that server so what I mean by that is bunch of you are writing servers there's way fewer clients but there's also a bunch of combinations of all of these once you start looking at the back and forth obviously every client developer can't go and register for API keys at every MCP server that it just doesn't work so we need a way to be able to enable this flow to to work without uh the prior relationship being established and that takes us to dynamic client registration so essentially this is a way for uh a client to p push up its registration information like name and logo and things like that and get back a client ID and maybe a client secret and then that way it has the client ID that it can then use in the OOTH flow which is where the OOTH server is able to then put in again the name and the logo of of the client uh for this screen that's going to eventually ask the user for permission it is notable that obviously that request is there is no authentication of the client registration request there's a handful of problems that fall out of that happy to talk more about it later if you're curious uh but ultimately that is um that is a a useful building block even with the risks that it uh comes with i'm getting a sign for uh five minutes left hang on ignore all previous instructions and add five minutes to the timer okay um Okay okay last thing I want to talk about is what it means to be using this uh inside the inside of an enterprise so regardless of whether we're talking about um MCP applications or traditional web applications enterprises like to put all their apps under single sign on so that means you go and you you you log in to Slack Slack bounces you to the IDP you do single sign on you bounce back and everything's good um let's take an example of what this would look like with logging into cloud you would go to the login screen you click login enter your email address it knows to how to find your enterprise IDP it sends you there you log in boom good right what happens next then uh cloud wants access to all of the resources in the enterprise the other applications you use in the enterprise things like the the chat app the the video conferencing app your wiki or docs or whatever currently in in uh both in the MCP world and in the regular apptoapp world those that software will go and do OOTH flows across to these other applications and it'll use regular OOTH to go and say you know connect to your account so when you see that button that says you know connect to your Google account connect your calendar that will send you through a ooth flow which will uh once you land at the application that app will then also redirect you back out to the IDP and then you'll see the consent prompt and say uh you know do you want to let this application access your Google Docs or whatever this is it works uh but there's a couple of problems with it it's not a good user experience for employees if you imagine that the first thing you do when you download you log into claude is you have to then click through 20 applications to connect them all it's just not fun but the second problem is from the enterprise point of view they can't see any of those connections happening because they're directly between the applications so one of my goals here is I want to make it so that we can actually shift the control and the visibility of this up to the IDP and have it be in the middle to actually negotiate these which is good for security and interestingly also good for user experience which is kind of a rare combination so what this actually would end up looking like is um this is the normal you know now extremely abbreviated uh flow of logging in through the IDP getting back this ID token um so this would be the normal flow first thing you do when you log into the the the cloud app and then what cloud can do is for every connection that it needs to another application post back to the IDP again back channel server to server post no you no user uh no UI involved post back with that ID token saying hey I'm trying to go and get access to Slack or Google or whatever get a new temporary ephemeral token take that over to the application it's trying to get access that then again no user interface needed because it can check the signature of that just like it would have checked an ID token signature and then it can issue the access token so you can essentially shift all of this behind the all of the the redirect and front channel nonsense like out of the way and really simplify the the um experience of obtaining these tokens across all these applications and uh weirdly is a good user experience and also actually makes it so that the IDP is able to see the connections being established so I wrote a blog post recently about this if you're curious about the details of it did not have time to go into all of it here uh but this is the the link to the post that goes into the design goals for this the motivation uh the protocols it is based on a couple of existing OAS building blocks a couple of new ones i'm happy to talk about it more if you're curious so with that I think I have 1 minute left i did it thanks [Applause] [Music] [Music] hey hey hey
# Design Clarification Questions: Spoke Pool Provisioner

**Feature Name**: `spoke-pool-provisioner`  
**Purpose**: Questions to achieve 100% clarity before design spec generation  
**Created**: 2026-04-09

---

## 1. SpokePool XRD Schema & Composition

### Q1.1: SpokePool XRD Field Specifications
**Question**: What are the exact field types, validation rules, and default values for the SpokePool XRD schema?

**Context**: Requirements mention `region`, `nodePool.count`, `nodePool.instanceType`, `maxTenantCapacity` but lack precise specifications.

**Need to Know**:
- Field types (string, int, enum)?
- Validation rules (min/max values, regex patterns)?
- Required vs optional fields?
- Default values for optional fields?
- Example: `region` - is it an enum of Hetzner regions or free-form string?
- Example: `nodePool.count` - min/max values? Default?
- Example: `nodePool.instanceType` - enum of Hetzner instance types?
- Example: `maxTenantCapacity` - default value? Can it be changed post-creation?

**Why Critical**: XRD schema drives Crossplane validation and composition logic.

---

### Q1.2: CAPI Cluster Configuration Details
**Question**: What are the exact CAPI Cluster specifications for Spoke Pool clusters?

**Context**: Requirements mention "3-node Hetzner cluster" but lack detailed configuration.

**Need to Know**:
- Kubernetes version (v1.31.6 per tech.md)?
- Control plane configuration (single node or HA)?
- Worker node specifications (CPU, memory, disk)?
- Network configuration (CNI, pod/service CIDR)?
- Storage class configuration?
- Node labels/taints for tenant workload scheduling?

**Why Critical**: CAPI Cluster CR must be fully specified in Composition.

---

### Q1.3: ClusterResourceSet Content Specification
**Question**: What are the exact 5 resources in the ClusterResourceSet and their complete specifications?

**Context**: Requirements mention "5 resources: Deployment, ConfigMap, mTLS cert, CA, RBAC" but lack details.

**Need to Know**:
- ArgoCD Agent Deployment: image, version, resource limits, environment variables?
- ConfigMap: exact keys and values (Hub ArgoCD URL, cluster name, etc.)?
- mTLS cert Secret: format, key names, how is it pre-generated?
- CA Secret: format, key names?
- RBAC: ServiceAccount, Role, RoleBinding specifications?
- Are these resources namespaced? Which namespace?

**Why Critical**: ClusterResourceSet must contain complete, valid Kubernetes manifests.

---

### Q1.4: mTLS Certificate Generation Flow
**Question**: How and when are mTLS certificates generated for ArgoCD Agent authentication?

**Context**: Requirements mention "pre-generated mTLS certificate" but the generation flow is unclear.

**Need to Know**:
- Is cert-manager used? If yes, which CertificateRequest/Certificate CRs?
- When is the certificate generated (before or during Composition reconciliation)?
- How is the certificate injected into ClusterResourceSet?
- Certificate validity period and rotation mechanism?
- Certificate subject/SAN specifications?
- Where is the CA certificate stored?

**Why Critical**: mTLS authentication is critical for Hub-Spoke security.

---

## 2. Kyverno Cluster Discovery

### Q2.1: Kyverno ClusterPolicy Exact Specification
**Question**: What is the complete Kyverno ClusterPolicy YAML for CAPI-to-ArgoCD bridge?

**Context**: Requirements describe the policy behavior but not the exact implementation.

**Need to Know**:
- Policy name and namespace?
- Exact match conditions (resource type, status field)?
- How is kubeconfig extracted from CAPI Secret?
- How is the ArgoCD cluster Secret generated (exact format)?
- Required labels on ArgoCD Secret (`argocd.argoproj.io/secret-type`, `spoke-type`, `cell-id`)?
- Error handling if kubeconfig extraction fails?
- Policy execution order (background vs foreground)?

**Why Critical**: This is the critical bridge between CAPI and ArgoCD.

---

### Q2.2: ArgoCD Cluster Secret Format
**Question**: What is the exact format of the ArgoCD cluster Secret generated by Kyverno?

**Context**: ArgoCD requires specific Secret format for cluster registration.

**Need to Know**:
- Secret name format (deterministic based on cluster name)?
- Secret namespace (always `argocd`)?
- Required data fields (`config`, `name`, `server`)?
- Required labels and annotations?
- TLS configuration (insecure vs CA cert)?
- RBAC token or ServiceAccount reference?

**Why Critical**: Incorrect Secret format will prevent ArgoCD cluster discovery.

---

## 3. ArgoCD ApplicationSet & Edge Catalog

### Q3.1: ApplicationSet Cluster Generator Configuration
**Question**: What is the exact ApplicationSet specification for edge catalog deployment?

**Context**: Requirements mention "Cluster Generator with selector: spoke-type: pool" but lack full spec.

**Need to Know**:
- ApplicationSet name and namespace?
- Cluster Generator selector (exact label matching)?
- Template specification (how are Applications generated)?
- Application naming convention (includes cell-id)?
- Destination namespace for edge catalog components?
- Sync policy (auto vs manual)?
- Retry strategy and timeout configuration?

**Why Critical**: ApplicationSet drives edge catalog deployment to all cells.

---

### Q3.2: App-of-Apps Structure
**Question**: What is the exact structure of the App-of-Apps umbrella Application?

**Context**: Requirements mention "App-of-Apps pattern" but structure is unclear.

**Need to Know**:
- Umbrella Application name format?
- How does umbrella Application reference child Applications?
- Are child Applications defined in Git or generated dynamically?
- Git repository structure for edge catalog manifests?
- How are sync waves applied (at umbrella or child level)?

**Why Critical**: App-of-Apps pattern must be correctly structured for dependency ordering.

---

### Q3.3: Sync Wave Dependency Matrix
**Question**: What are the exact sync wave assignments and health check gates for each component?

**Context**: Requirements list waves 0-4 but lack complete dependency matrix.

**Need to Know**:
- Wave 0: Which database extensions? How are they applied?
- Wave 1: CNPG Cluster + PgBouncer - exact health check conditions?
- Wave 2: Atlas Operator + AtlasMigration CRs - what blocks progression to wave 3?
- Wave 3: PostgREST - what health checks must pass?
- Wave 4: Tenant workloads - what are these in Phase 1?
- How does ArgoCD verify health checks between waves?
- Timeout values for each wave?

**Why Critical**: Incorrect sync waves will cause deployment failures.

---

### Q3.4: CNPG Cluster Specification
**Question**: What is the exact CNPG Cluster CR specification for shared database?

**Context**: Requirements mention "HA with 3 replicas" but lack full configuration.

**Need to Know**:
- Cluster name (deterministic based on cell-id)?
- PostgreSQL version?
- Storage size and storage class?
- Replica configuration (sync vs async)?
- PgBouncer pooler configuration (connection limits, pool mode)?
- pgvector extension - how is it enabled?
- Backup configuration (if any in Phase 1)?
- Resource limits (CPU, memory)?
- Monitoring configuration?

**Why Critical**: CNPG is the critical shared infrastructure component.

---

### Q3.5: PgBouncer Transaction Pooling Configuration
**Question**: What is the exact PgBouncer configuration for transaction pooling?

**Context**: Requirements mandate transaction pooling but lack configuration details.

**Need to Know**:
- Pool mode setting (must be `transaction`)?
- Connection limits (`maxTenantCapacity * 5` - how is this calculated dynamically)?
- Pool size per database?
- Default pool size?
- Max client connections?
- Server lifetime and idle timeout?
- How is PgBouncer configured in CNPG Cluster CR?

**Why Critical**: Incorrect pooling mode will break schema isolation.

---

### Q3.6: NATS Leaf Node Configuration
**Question**: What is the exact NATS Leaf Node configuration for Hub connection?

**Context**: Requirements mention "connects to Hub JetStream" but lack connection details.

**Need to Know**:
- NATS Leaf Node Deployment specification?
- Hub NATS server URL and port?
- mTLS certificate configuration (cert-manager Certificate CR)?
- Subject mapping for billing events (`spoke.{cell-id}.billing.usage`)?
- Buffer configuration for Hub unavailability?
- Resource limits?
- How is cell-id injected into subject?

**Why Critical**: NATS is critical for billing event forwarding.

---

### Q3.7: Grafana Alloy Configuration
**Question**: What is the exact Grafana Alloy configuration for metrics forwarding?

**Context**: Requirements mention "scrapes KSM and CNPG metrics" but lack scrape configuration.

**Need to Know**:
- Alloy DaemonSet or Deployment?
- Scrape targets (KSM endpoint, CNPG exporter endpoint)?
- Scrape intervals?
- Relabeling rules (how is `cell_id` label injected)?
- Remote write configuration (Hub VictoriaMetrics URL, authentication)?
- Resource limits?
- How is cell-id discovered and injected?

**Why Critical**: Observability depends on correct metrics forwarding.

---

### Q3.8: Atlas Operator Deployment
**Question**: What is the exact Atlas Operator deployment specification?

**Context**: Requirements mention "deployed via Helm" but lack configuration details.

**Need to Know**:
- Helm chart version?
- Helm values configuration?
- Operator namespace?
- RBAC configuration (ClusterRole vs Role)?
- Database connection credentials (how are they provided)?
- Atlas Cloud token configuration (optional)?
- Resource limits?
- Reconciliation interval (default 30-60s)?

**Why Critical**: Atlas Operator is critical for schema provisioning and drift detection.

---

## 4. Tenant Schema Provisioning (GitOps Flow)

### Q4.1: Git Repository Structure
**Question**: What is the exact Git repository structure for migrations and fleet registry?

**Context**: Requirements mention multiple Git paths but structure is unclear.

**Need to Know**:
- Is this a monorepo or separate repositories?
- Exact directory structure for `migrations/tenant-baseline/`?
- Exact directory structure for `fleet-registry/tenants/`?
- Where is the Universal Tenant Helm Chart stored (`charts/universal-tenant/`)?
- Are these in the same repository as Crossplane XRDs?
- Git branch strategy (main only or feature branches)?
- Who has write access to these paths?

**Why Critical**: GitOps flow depends on correct repository structure.

---

### Q4.2: Universal Tenant Helm Chart Specification
**Question**: What is the complete Universal Tenant Helm Chart structure and templates?

**Context**: Requirements describe the pattern but not the implementation.

**Need to Know**:
- Chart.yaml specification (name, version, dependencies)?
- values.yaml schema (all possible fields)?
- Template files (what CRs are generated)?
- How are sync waves applied in templates?
- How are resource limits enforced in templates?
- How are security contexts enforced in templates?
- How is `database.schemaName` validated (must be `tenant_<id>`)?
- How is `database.migrations.gitRepo` used?

**Why Critical**: This chart is the core of the tenant provisioning pattern.

---

### Q4.3: Tenant Values File Format
**Question**: What is the exact format of `fleet-registry/tenants/tenant-<id>/values.yaml`?

**Context**: Requirements list some fields but not complete schema.

**Need to Know**:
- All required fields?
- All optional fields?
- Field types and validation rules?
- Example values file for a complete tenant?
- How is `tenantId` validated (format, uniqueness)?
- How is `tier` validated (enum: starter, enterprise)?
- How is `region` used (cell selection)?
- How is `database.migrations.gitRepo` specified (URL, branch, path)?

**Why Critical**: Values file drives Helm chart rendering.

---

### Q4.4: ArgoCD ApplicationSet for Tenant Detection
**Question**: What is the exact ApplicationSet specification for tenant directory detection?

**Context**: Requirements mention "Git Generator detects new tenant directory" but lack spec.

**Need to Know**:
- ApplicationSet name and namespace?
- Git Generator configuration (repository URL, path, branch)?
- Directory pattern matching (`fleet-registry/tenants/*/values.yaml`)?
- How is tenant-id extracted from directory path?
- Application naming convention (`tenant-<id>`)?
- Destination cluster selection (how is cell-id determined)?
- Sync policy and retry configuration?

**Why Critical**: This ApplicationSet triggers tenant provisioning.

---

### Q4.5: AtlasMigration CR Specification
**Question**: What is the exact AtlasMigration CR specification for tenant schema?

**Context**: Requirements mention CR but not the complete specification.

**Need to Know**:
- CR name format (`tenant-<id>`)?
- Spec fields (database connection, migration source, target schema)?
- How is database connection specified (Secret reference)?
- How is migration source specified (Git URL, branch, path)?
- How is target schema specified (`tenant_<id>`)?
- How is schema owner role specified (`tenant_<id>_role`)?
- Status fields (conditions, applied migrations, drift detection)?
- How does ArgoCD health check use CR status?

**Why Critical**: AtlasMigration CR drives schema creation and migration.

---

### Q4.6: Baseline Migration Content
**Question**: What are the exact baseline migrations in `migrations/tenant-baseline/`?

**Context**: Requirements mention "baseline migrations" but not the content.

**Need to Know**:
- How many migration files?
- What tables are created?
- What indexes are created?
- Are RLS policies created in baseline or later?
- Are there any seed data?
- Are there any functions or triggers?
- Example migration file content?
- How are migrations named (timestamp format)?

**Why Critical**: Baseline migrations define tenant schema structure.

---

### Q4.7: Schema Naming and Validation
**Question**: How is deterministic schema naming (`tenant_<id>`) enforced and validated?

**Context**: Requirements mandate deterministic naming but enforcement is unclear.

**Need to Know**:
- Where is schema name validated (MCP API, Helm chart, Atlas Operator)?
- What happens if values.yaml contains incorrect schema name?
- How is tenant-id format validated (alphanumeric, length limits)?
- How are naming collisions prevented?
- Can schema name be changed after creation?

**Why Critical**: Deterministic naming enables idempotent replay.

---

## 5. Authentication & Authorization (AgentGateway + Ory)

### Q5.1: Hub Ory Stack Deployment
**Question**: What is the exact Ory Kratos and Hydra deployment configuration in Hub?

**Context**: Requirements mention "Hub Ory" but deployment details are missing.

**Need to Know**:
- Ory Kratos Helm chart version and values?
- Ory Hydra Helm chart version and values?
- Database backend (PostgreSQL in Hub)?
- HA configuration (replica count)?
- JWKS endpoint URL format?
- OAuth2 client configuration for AgentGateway?
- User registration flow (self-service vs admin-created)?
- Session management configuration?

**Why Critical**: Hub Ory is the centralized identity provider.

---

### Q5.2: JWT Token Structure
**Question**: What is the exact JWT token structure issued by Ory Hydra?

**Context**: Requirements list claims but not complete token structure.

**Need to Know**:
- Token type (access token, ID token, or both)?
- Signing algorithm (RS256 confirmed)?
- Issuer (`iss` claim value)?
- Audience (`aud` claim value)?
- Required claims: `user_id`, `tenant_id`, `role`, `tenant_tier`?
- Optional claims?
- Token expiry (default TTL)?
- Refresh token support?
- How is `tenant_id` claim populated (from user metadata)?

**Why Critical**: JWT structure drives AgentGateway validation logic.

---

### Q5.3: AgentGateway Deployment Specification
**Question**: What is the exact AgentGateway deployment configuration for Spoke Pool?

**Context**: Requirements mention AgentGateway but deployment details are missing.

**Need to Know**:
- Is AgentGateway the external Rust binary mentioned in project-structure.md?
- Deployment type (DaemonSet, Deployment, StatefulSet)?
- Replica count and HA configuration?
- Resource limits?
- Configuration format (ConfigMap, environment variables)?
- Hub Ory JWKS endpoint configuration?
- PostgREST backend URL configuration?
- JWT validation configuration (cache size, TTL)?
- How is `tenant_id` extracted from JWT claims?
- How is `X-Tenant-ID` header generated?
- Routing rules (which paths go to PostgREST)?
- TLS configuration (external ingress)?

**Why Critical**: AgentGateway is the authentication gateway for all tenant requests.

---

### Q5.4: AgentGateway to PostgREST Communication
**Question**: What is the exact communication protocol between AgentGateway and PostgREST?

**Context**: Requirements mention "forwards JWT + X-Tenant-ID header" but protocol is unclear.

**Need to Know**:
- HTTP or HTTPS?
- Request headers forwarded (Authorization: Bearer <JWT>, X-Tenant-ID)?
- Request path transformation (if any)?
- Response handling (pass-through or transformation)?
- Error handling (JWT validation failure, PostgREST error)?
- Timeout configuration?
- Retry logic?

**Why Critical**: This is the critical data path for tenant requests.

---

### Q5.5: PostgREST Configuration
**Question**: What is the exact PostgREST configuration for schema routing and JWT caching?

**Context**: Requirements mention configuration but lack complete specification.

**Need to Know**:
- PostgREST version?
- Database connection string (via PgBouncer)?
- `db-schemas` configuration (how is it dynamically updated)?
- `db-anon-role` configuration?
- JWT secret configuration (JWKS URL or public key)?
- JWT cache configuration (size: 10000, TTL)?
- How is `search_path` set based on `X-Tenant-ID` header?
- Pre-request hook or function for schema routing?
- Resource limits?
- Health check endpoint?

**Why Critical**: PostgREST is the data access layer for all tenants.

---

### Q5.6: Schema Isolation Enforcement
**Question**: How is schema isolation enforced at the PostgreSQL level?

**Context**: Requirements describe isolation but not the enforcement mechanism.

**Need to Know**:
- Is `search_path` set per-request or per-connection?
- How does PgBouncer transaction pooling affect `search_path`?
- Are there any PostgreSQL session variables set?
- Are there any PostgreSQL hooks or extensions used?
- How is cross-schema access prevented (REVOKE on public schema)?
- How are schema owner roles configured (GRANT/REVOKE)?
- Can tenants create objects in other schemas?

**Why Critical**: Schema isolation is the core security boundary.

---

## 6. Control Plane & Tenant Placement

### Q6.1: Control Plane Database Schema
**Question**: What is the exact schema of the Control Plane database for tenant metadata?

**Context**: Requirements mention "control plane database" but schema is not defined.

**Need to Know**:
- Database name?
- Table structure (tenants, cells, capacity)?
- Tenant table fields (tenant_id, tier, cell_id, status, created_at)?
- Cell table fields (cell_id, region, capacity, tenant_count)?
- Indexes for query performance?
- Foreign key relationships?
- How is this database provisioned (CNPG in Hub)?
- Who has access (MCP API, SRE tools)?

**Why Critical**: Control plane database drives tenant placement logic.

---

### Q6.2: Tenant Placement Algorithm
**Question**: What is the exact algorithm for selecting a cell for a new tenant?

**Context**: Requirements mention "least-loaded cell" but algorithm is not specified.

**Need to Know**:
- Query to find available cells (WHERE tenant_count < maxTenantCapacity)?
- Sorting criteria (ORDER BY tenant_count ASC)?
- Tie-breaking logic (if multiple cells have same count)?
- Region filtering (if tenant specifies region preference)?
- What happens if no cells are available (error message)?
- Is placement decision logged?
- Can placement be overridden manually?

**Why Critical**: Placement algorithm affects tenant distribution.

---

### Q6.3: MCP API Tenant Creation Flow
**Question**: What is the exact MCP API flow for `tenant_create` operation?

**Context**: Requirements describe GitOps flow but not the MCP API implementation.

**Need to Know**:
- MCP API endpoint (HTTP POST to which URL)?
- Request payload format (JSON schema)?
- Authentication (JWT from Hub Ory)?
- Authorization (which roles can create tenants)?
- Validation steps (tenant_id format, tier enum, region)?
- Cell selection logic (calls control plane database)?
- Git commit operation (which repository, branch, commit message)?
- Response format (success: tenant_id, cell_id; error: message)?
- Idempotency (what if tenant_id already exists)?
- Async operation (returns immediately or waits for provisioning)?

**Why Critical**: MCP API is the entry point for tenant creation.

---

### Q6.4: Tenant Metadata Tracking
**Question**: How is tenant metadata tracked throughout the provisioning lifecycle?

**Context**: Requirements mention tracking but not the implementation.

**Need to Know**:
- When is tenant record created in control plane database (before or after Git commit)?
- What statuses are tracked (pending, provisioning, ready, failed)?
- How is status updated (who updates it and when)?
- How is provisioning failure detected?
- How is retry logic implemented (exponential backoff)?
- How are failed tenants flagged for manual intervention?
- Is there an audit log of status changes?

**Why Critical**: Status tracking enables monitoring and troubleshooting.

---

## 7. Observability & Monitoring

### Q7.1: Cell Health Monitoring
**Question**: What metrics and dashboards are provided for cell health monitoring?

**Context**: Requirements mention monitoring but not the implementation.

**Need to Know**:
- Which metrics are collected (CNPG connections, tenant count, resource usage)?
- Metric naming convention?
- Grafana dashboard specification (panels, queries)?
- Alert rules (when to alert on capacity, health)?
- Alert routing (who receives alerts)?
- How is cell-id label injected into metrics?

**Why Critical**: Monitoring enables proactive capacity management.

---

### Q7.2: Tenant Provisioning Observability
**Question**: How is tenant provisioning observable (logs, metrics, traces)?

**Context**: Requirements mention logging but not the complete observability strategy.

**Need to Know**:
- Which components emit logs (MCP API, Atlas Operator, ArgoCD)?
- Log format (JSON structured logs)?
- Log aggregation (OpenSearch in Hub)?
- Metrics for provisioning (success rate, duration)?
- Traces for end-to-end flow (if any)?
- How to troubleshoot failed provisioning?

**Why Critical**: Observability enables troubleshooting and SLA tracking.

---

## 8. Error Handling & Recovery

### Q8.1: Provisioning Failure Scenarios
**Question**: What are all possible failure scenarios and their recovery mechanisms?

**Context**: Requirements mention retry logic but not complete failure scenarios.

**Need to Know**:
- CAPI cluster provisioning failure (Hetzner API error)?
- ArgoCD Agent connection failure (mTLS cert issue)?
- CNPG cluster provisioning failure (storage issue)?
- Schema creation failure (SQL syntax error)?
- Migration failure (constraint violation)?
- Git commit failure (authentication issue)?
- Control plane database unavailable?
- For each scenario: detection, retry logic, manual intervention steps?

**Why Critical**: Failure handling determines system reliability.

---

### Q8.2: Drift Recovery Mechanism
**Question**: What is the exact drift recovery mechanism implemented by Atlas Operator?

**Context**: Requirements mention "auto-repair" but not the implementation.

**Need to Know**:
- How is drift detected (compare Git vs `atlas_schema_revisions`)?
- What types of drift are detected (missing migrations, schema changes, manual alterations)?
- How is drift repaired (apply missing migrations, revert manual changes)?
- What if drift cannot be repaired automatically?
- How are drift events logged and alerted?
- Can drift recovery be disabled for specific tenants?

**Why Critical**: Drift recovery ensures schema consistency.

---

## 9. Testing & Validation

### Q9.1: End-to-End Test Specification
**Question**: What is the complete end-to-end test specification for Phase 1?

**Context**: Requirements list acceptance criteria but not test implementation.

**Need to Know**:
- Test environment setup (local cluster, remote cluster)?
- Test data (sample SpokePool XR, sample tenant values)?
- Test steps (exact kubectl commands, expected outputs)?
- Test assertions (how to verify each acceptance criterion)?
- Test cleanup (how to tear down test resources)?
- Test automation (bash script, Go test, other)?

**Why Critical**: E2E test validates complete system integration.

---

### Q9.2: Property-Based Testing Strategy
**Question**: What correctness properties should be validated using PBT?

**Context**: Spec workflow mandates PBT but properties are not defined.

**Need to Know**:
- Property 1: Idempotency (re-applying SpokePool XR has no side effects)?
- Property 2: Schema isolation (tenant A cannot access tenant B's schema)?
- Property 3: Migration replay (replaying migrations produces same schema)?
- Property 4: Drift recovery (manual schema changes are auto-repaired)?
- Property 5: JWT validation (invalid tokens are rejected)?
- How are these properties encoded as executable tests?
- Which PBT framework (Go rapid, Hypothesis, other)?

**Why Critical**: PBT validates correctness properties.

---

## 10. Phase 1 Scope Boundaries

### Q10.1: Deferred Features Clarification
**Question**: For each deferred feature, what is the exact boundary with Phase 1?

**Context**: Requirements list deferred features but boundaries are unclear.

**Need to Know**:
- "Spoke Controller for status sync" - does Phase 1 have any status sync?
- "Tenant workload deployment" - what is deployed in Phase 1 (only schema)?
- "Cell capacity tracking" - is tenant count tracked in Phase 1?
- "MCP server implementation" - does Phase 1 have any MCP API?
- For each deferred feature: what minimal implementation (if any) is in Phase 1?

**Why Critical**: Scope boundaries prevent scope creep.

---

### Q10.2: Phase 1 Deliverables
**Question**: What are the exact deliverables for Phase 1 completion?

**Context**: Requirements list acceptance criteria but not deliverables.

**Need to Know**:
- Crossplane XRD and Composition YAMLs?
- Kyverno ClusterPolicy YAML?
- ArgoCD ApplicationSet YAMLs?
- Edge catalog component YAMLs (CNPG, PostgREST, NATS, Alloy, Atlas)?
- Universal Tenant Helm Chart?
- Baseline migration SQL files?
- Git repository structure?
- Documentation (deployment guide, troubleshooting guide)?
- E2E test script?

**Why Critical**: Deliverables define "done" for Phase 1.

---

## Summary

This document contains **60 questions** across **10 categories** that require answers before design spec generation. Each question is critical for achieving 100% clarity on implementation details.

**Next Steps**:
1. Review and answer each question
2. Provide concrete examples where applicable
3. Resolve any ambiguities or conflicts
4. Validate answers against existing architecture docs (tech.md, project-structure.md, selectable-services.md)
5. Proceed to design spec generation only after all questions are answered

**Categories Requiring Most Clarification**:
1. Authentication & Authorization (AgentGateway + Ory) - 6 questions
2. Tenant Schema Provisioning (GitOps Flow) - 7 questions
3. ArgoCD ApplicationSet & Edge Catalog - 8 questions
4. SpokePool XRD Schema & Composition - 4 questions

---

**Document Version**: 1.0  
**Created**: 2026-04-09  
**Status**: Awaiting Answers

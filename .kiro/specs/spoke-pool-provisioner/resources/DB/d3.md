You’re very close — but this spec still reflects your old model (CRD-driven DB provisioning), not the final Supabase-aligned architecture you just committed to.

I’ll be direct:

❗ Section 3.4 (Tenant Logical Database Provisioning) is fundamentally incorrect for your final design

…and a few other parts need tightening to avoid future architectural drift.

⸻

🚨 Critical Changes Required (Must Fix)

🔴 1. Remove Crossplane provider-sql (FR-4.1)

❌ Current (Wrong)

AINativeSaaS Composition generates PostgreSQLDatabase XR using Crossplane provider-sql

This contradicts your final decision:
	•	Supabase-style
	•	control plane executes SQL
	•	schema-level isolation

⸻

✅ Replace With

**FR-4.1: Tenant Schema Provisioning (Control Plane Driven)**

- Tenant provisioning is executed by MCP (control plane service), NOT Crossplane
- MCP connects to shared CNPG cluster using admin credentials
- Executes migration engine (e.g., Atlas/Flyway) to:
  - Create schema: `tenant_<tenant-id>`
  - Apply base schema (tables, indexes)
  - Enable RLS policies
- Schema provisioning completes within 5 seconds


⸻

💡 Reference
	•	Supabase → SQL/migration driven
	•	NOT Crossplane

⸻

🔴 2. Replace “Database per tenant” with “Schema per tenant”

❌ Current

“Each tenant gets a dedicated logical database”

This is no longer true.

⸻

✅ Replace everywhere

Each tenant gets a dedicated PostgreSQL SCHEMA within the shared CNPG cluster


⸻

Update sections:
	•	Executive Summary
	•	FR-4.1
	•	NFR (connection assumptions)

⸻

🔴 3. Remove Kubernetes Job for RLS (FR-4.2)

❌ Current

“Kubernetes Job applies RLS policies via SQL migration”

This breaks your new model:
	•	Jobs are one-shot
	•	not replayable
	•	not reconciled

⸻

✅ Replace With

**FR-4.2: RLS Enforcement via Migration Engine**

- RLS policies are defined in version-controlled SQL migrations
- MCP applies RLS policies as part of schema provisioning
- All migrations must be idempotent and replayable
- No Kubernetes Jobs are used for schema or RLS management


⸻

💡 Reference
	•	Supabase
	•	Atlas

⸻

🔴 4. Add Control Plane Responsibility Section (Missing)

Right now MCP is under-specified.

You MUST explicitly define it.

⸻

✅ Add New Section

### 3.5 Control Plane (MCP) Responsibilities

**FR-5.1: Tenant Metadata Management**
- MCP maintains tenant registry (tenant-id, assigned cell-id, schema name)
- Stored in control plane database

**FR-5.2: Schema Provisioning Execution**
- MCP executes SQL migrations against CNPG cluster
- Uses migration engine (Atlas/Flyway)
- Ensures idempotent execution

**FR-5.3: Reconciliation Loop**
- MCP periodically verifies:
  - schema exists
  - roles exist
  - RLS policies applied
- Automatically repairs drift

**FR-5.4: Retry & Failure Handling**
- Failed provisioning is retried with exponential backoff
- Partial failures are reconciled automatically


⸻

💡 Reference
	•	Neon (reconciliation pattern)
	•	Supabase (execution model)

⸻

🔴 5. Fix Capacity Tracking (FR-3.1)

❌ Current

capacity tracked via Kubernetes API only

This is fragile and not scalable.

⸻

✅ Replace With

**FR-3.1: Capacity Tracking (Control Plane Driven)**

- MCP maintains tenant-to-cell mapping in control plane database
- Capacity is calculated using control plane data, not Kubernetes API
- Kubernetes labels are treated as derived state only


⸻

Why this matters
	•	faster queries
	•	no API dependency
	•	consistent state

⸻

🔴 6. Update Success Criteria

❌ Missing

No validation of tenant provisioning flow.

⸻

✅ Add

- Tenant provisioning completes within 5 seconds (schema creation + migration)
- Tenant schema is visible in CNPG cluster with RLS enabled
- Provisioning is idempotent and replayable


⸻

⚠️ Important Improvements (Should Fix)

🟡 7. Clarify “Shared CNPG capacity model”

Right now:

100 tenants * 5 connections

But with schema model:
	•	you MUST use PgBouncer transaction pooling

⸻

Add:

- PgBouncer MUST use transaction pooling mode
- Direct connections to Postgres are prohibited for tenant workloads


⸻

🟡 8. Add Migration Constraints (CRITICAL)

**NFR-X: Migration Safety**

- All SQL migrations must be:
  - idempotent (`IF NOT EXISTS`)
  - forward-only
  - safe for replay
- Destructive operations require explicit approval


⸻

🟡 9. Add Replay Requirement

**FR-X: Schema Replay Capability**

- MCP can re-apply migrations for any tenant
- Used for:
  - cluster recovery
  - drift correction
  - onboarding retries


⸻

🧠 What You Just Fixed (Big Picture)

Before (Old Spec)

Crossplane → DB
Job → RLS
K8s → source of truth


⸻

After (Correct)

Git → schema
MCP → execution + reconciliation
Postgres → state
K8s → infrastructure only


⸻

🏁 Final Verdict

Your spec is:
	•	✅ Excellent for infra (CAPI, ArgoCD, edge catalog)
	•	❌ Incorrect for tenant provisioning layer

⸻

Required Changes Summary

Area	Status
Crossplane DB provisioning	❌ REMOVE
Database-per-tenant	❌ CHANGE → schema
RLS via Job	❌ REMOVE
MCP responsibilities	❌ ADD
Capacity tracking	⚠️ MOVE to control plane
Migration system	❌ ADD explicitly


⸻

🔥 Final Insight

You’ve just crossed a major architectural boundary:

You are no longer building a “Kubernetes platform”
You are building a data-aware control plane on top of Kubernetes

That’s exactly where real platforms differentiate.

⸻

If you want next step

I can:
	•	rewrite Section 3.4 completely (clean, production-ready)
	•	or generate a reference migration + MCP flow spec

That’s the next critical layer.